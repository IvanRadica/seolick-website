---
// SEO LICK — locked letter-reveal hero, v2 (approved by Ivan 2026-07-04,
// source: LightVersion/hero-v2-test.html).
// v2 changes vs v1: letters spell the full brand S·E·O·L·I·C·K (descriptors
// removed), per-letter step 375ms so the 7th letter lands at 2650ms — the
// same absolute time as v1's 4th letter — and every downstream beat
// (turnRed, divider, tagline, logo, scroll hint) keeps the locked timings.
// Only L-I-C-K flip to red, matching the wordmark. Do not change the
// choreography.
interface Props {
  tagline?: string;
  scrollLabel?: string;
}
const { tagline = 'We <em>LICK</em> websites. For organic growth.', scrollLabel = 'Scroll' } = Astro.props;

const letters = ['S', 'E', 'O', 'L', 'I', 'C', 'K'];
---

<section class="hero-lick" id="hero" aria-label="SEO LICK">
  <div class="letters" id="letters">
    {letters.map((ch) => (
      <div class="letter-group" id={`lg-${ch.toLowerCase()}`}>
        <div class="letter" id={`lt-${ch.toLowerCase()}`}>{ch}</div>
      </div>
    ))}
  </div>

  <div class="divider-wrap">
    <div class="divider-line" id="divider"></div>
  </div>

  <div class="tagline" id="tagline" set:html={tagline}></div>

  <div class="scroll-hint" id="scroll-hint">
    <span class="scroll-hint-label">{scrollLabel}</span>
    <div class="scroll-line"></div>
  </div>

  <button class="hero-replay" id="hero-replay" type="button" aria-label="Replay animation">&#8635; Replay</button>
</section>

<style is:global>
  /* ── Locked SEOLICK hero v2 — namespaced under .hero-lick, keyframes global
       so the runtime JS can assign them by name. ── */
  .hero-lick {
    --red: #e12c2c;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    position: relative;
    padding: 3rem 1.25rem 4rem;
    background: #fff;
  }
  .hero-lick .letters {
    display: flex;
    gap: clamp(8px, 1.8vw, 36px);
    align-items: center;
  }
  .hero-lick .letter-group { opacity: 0; }
  .hero-lick .letter {
    font-family: "Montserrat", sans-serif;
    font-size: clamp(40px, 8.8vw, 128px);
    font-weight: 900;
    line-height: 1;
    letter-spacing: -0.02em;
    color: #111;
  }
  .hero-lick .divider-wrap {
    width: clamp(300px, 70vw, 900px);
    margin: 48px 0 40px;
    height: 1px;
    overflow: hidden;
  }
  .hero-lick .divider-line {
    width: 0%;
    height: 100%;
    background: var(--red);
    transition: width 0.8s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .hero-lick .tagline {
    font-family: "Montserrat", sans-serif;
    font-size: clamp(14px, 2vw, 22px);
    font-weight: 700;
    letter-spacing: 0.04em;
    color: #111;
    opacity: 0;
    transform: translateY(14px);
    text-align: center;
  }
  .hero-lick .tagline em { color: var(--red); font-style: normal; }
  .hero-lick .scroll-hint {
    position: absolute;
    bottom: 28px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    opacity: 0;
    transition: opacity 0.6s ease;
  }
  .hero-lick .scroll-hint.show { opacity: 1; }
  .hero-lick .scroll-hint-label {
    font-family: "Roboto Mono", monospace;
    font-size: 10px;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: rgba(0, 0, 0, 0.35);
  }
  .hero-lick .scroll-line {
    width: 1px;
    height: 36px;
    background: linear-gradient(to bottom, rgba(225, 44, 44, 0.7), transparent);
    animation: hl-scrollPulse 1.6s ease-in-out infinite;
  }
  .hero-lick .hero-replay {
    position: absolute;
    bottom: 28px;
    right: clamp(20px, 5vw, 80px);
    background: none;
    border: none;
    cursor: pointer;
    font-family: "Roboto Mono", monospace;
    font-size: 10px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: rgba(0, 0, 0, 0.35);
    padding: 8px;
    transition: color 0.2s ease;
  }
  .hero-lick .hero-replay:hover { color: var(--red); }

  /* Small screens: shrink letterforms so all seven letters + tagline fit. */
  @media (max-width: 520px) {
    .hero-lick .letters { gap: 6px; }
    .hero-lick .letter { font-size: clamp(30px, 10.5vw, 52px); }
    .hero-lick .tagline { font-size: 14px; padding: 0 10px; }
  }

  @keyframes hl-scrollPulse { 0%, 100% { opacity: 0.4; } 50% { opacity: 1; } }
  @keyframes letterIn {
    0%   { opacity: 0; transform: scale(1.5) translateY(-16px); filter: blur(8px); }
    60%  { filter: blur(0px); }
    100% { opacity: 1; transform: scale(1) translateY(0); }
  }
  @keyframes turnRed { from { color: #111; } to { color: var(--red); } }
  @keyframes taglineIn {
    from { opacity: 0; transform: translateY(14px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

  /* Reduced motion: skip the timeline, show the final resting state —
     S·E·O black, L·I·C·K red, everything visible. */
  @media (prefers-reduced-motion: reduce) {
    .hero-lick .letter-group { opacity: 1; }
    .hero-lick .letter { color: #111; }
    #lt-l, #lt-i, #lt-c, #lt-k { color: var(--red); }
    .hero-lick .divider-line { width: 100%; transition: none; }
    .hero-lick .tagline { opacity: 1; transform: none; }
    .hero-lick .scroll-hint { opacity: 1; }
    .hero-lick .scroll-line { animation: none; }
    .hero-lick .hero-replay { display: none; }
  }
</style>

<script is:inline>
  (function () {
    var reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    var replayBtn = document.getElementById('hero-replay');
    if (reduce) return; // resting state is handled by CSS

    // 375ms step lands the 7th letter (K) at 2650ms — identical to the v1
    // 4-letter timeline — so every downstream beat below stays locked.
    var STEP = 375;
    var order = ['s', 'e', 'o', 'l', 'i', 'c', 'k'];
    var steps = order.map(function (ch, i) {
      return { group: 'lg-' + ch, letter: 'lt-' + ch, t: 400 + STEP * i };
    });

    // Only L-I-C-K flip to red (brand wordmark).
    var redIds = ['lt-l', 'lt-i', 'lt-c', 'lt-k'];

    var timers = [];

    function runHero() {
      steps.forEach(function (s) {
        timers.push(setTimeout(function () {
          var g = document.getElementById(s.group);
          if (!g) return;
          void g.offsetWidth;
          g.style.animation = 'letterIn 0.5s cubic-bezier(0.22, 1, 0.36, 1) forwards';
        }, s.t));
      });

      var redAt = steps[steps.length - 1].t + 480; // 3130ms, same as v1

      timers.push(setTimeout(function () {
        redIds.forEach(function (id) {
          var el = document.getElementById(id);
          if (!el) return;
          void el.offsetWidth;
          el.style.animation = 'turnRed 0.35s ease forwards';
        });
      }, redAt));

      timers.push(setTimeout(function () {
        var d = document.getElementById('divider');
        if (!d) return;
        void d.offsetWidth;
        d.style.transition = 'width 0.9s cubic-bezier(0.4, 0, 0.2, 1)';
        d.style.width = '100%';
      }, redAt + 300));

      timers.push(setTimeout(function () {
        var t = document.getElementById('tagline');
        if (!t) return;
        void t.offsetWidth;
        t.style.animation = 'taglineIn 0.7s ease forwards';
      }, redAt + 900));

      timers.push(setTimeout(function () {
        var s = document.getElementById('scroll-hint');
        if (s) s.classList.add('show');
      }, redAt + 2400));
    }

    // Reset inline styles back to the pre-animation state, then rerun the
    // exact same locked timeline.
    function replayHero() {
      timers.forEach(clearTimeout);
      timers = [];
      steps.forEach(function (s) {
        var g = document.getElementById(s.group);
        var l = document.getElementById(s.letter);
        if (g) g.style.animation = '';
        if (l) l.style.animation = '';
      });
      var divider = document.getElementById('divider');
      if (divider) {
        divider.style.transition = 'none';
        divider.style.width = '0%';
        void divider.offsetWidth;
      }
      var tagline = document.getElementById('tagline');
      if (tagline) tagline.style.animation = '';
      var hint = document.getElementById('scroll-hint');
      if (hint) hint.classList.remove('show');
      requestAnimationFrame(function () { runHero(); });
    }

    if (replayBtn) replayBtn.addEventListener('click', replayHero);

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', runHero);
    } else {
      runHero();
    }
  })();
</script>
